# Daniel Zhu

Personal website. The live app is a React + TypeScript Vite project at the repo root.

```bash
npm install
npm run dev
```

Older versions live in `old-website/` and `version2-website/` until they are deleted.
